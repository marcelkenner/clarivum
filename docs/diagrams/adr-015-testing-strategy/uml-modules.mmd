```mermaid
classDiagram
    class TestEnvironmentFactory {
        +createNodeEnv()
        +loadEnvFile(path)
    }
    class FeatureFlagSeeder {
        +seedFlags(config)
        +resetFlags()
    }
    class AuroraFixtureManager {
        +applyFixtures()
        +cleanup()
    }
    class PlaywrightRunner {
        +launchPreview(url)
        +runSmokeSuite()
    }
    TestEnvironmentFactory --> FeatureFlagSeeder
    TestEnvironmentFactory --> AuroraFixtureManager
    PlaywrightRunner --> FeatureFlagSeeder
    PlaywrightRunner --> AuroraFixtureManager
```
