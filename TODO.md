# Clarivum TODOs

Centralized follow-up list for sprint guardrails and CI hygiene. Keep items concise and reference the source context so owners can respond quickly.

## Immediate Actions

1. **Branch protection:** Require the GitHub Actions check named `Validate, test, and smoke` on `main` so lint/typecheck/unit/smoke gates are enforced. (Source: TSK-PLAT-044 DoD)
2. **Slack webhook:** Populate/verify the `SLACK_WEBHOOK_CI` repository secret pointing to `#clarivum-platform` so Playwright failures alert the right channel. Document rotations in the deployment runbook. (Source: docs/runbooks/testing-stack.md)
3. **Task lint after tool install:** Once Node/npm are available in the local environment, run `npm run lint:tasks`, `npm run validate`, and `npm run test -- src/app/api/observability/v1/deployments/route.test.ts` to confirm the board/documentation updates and the new deployment webhook pass automation. (Source: follow-up from CI change)
4. **Metrics ingestion:** Extend the existing metrics job to read the new `ci-metrics.json` artifact and append duration/pass-fail data into `metrics/quality.json`. (Source: docs/QA/testing-strategy.md §Metrics & Reporting)
5. **Mission worker implementation:** Replace the placeholder in `backend/workers/otel-lambda-template.ts` (`// TODO: invoke mission business logic here`) with the actual mission event handler so the telemetry wrapper exercises real work. Link to the owning TSK/ADR when you wire it up.
6. **Environment secrets:** Populate the Grafana OTLP credentials, `NEXT_PUBLIC_OTEL_PROXY_URL`, and `OBSERVABILITY_DEPLOYMENT_SECRET` in each ECS/Lambda environment so the exporters and deployment webhook can emit telemetry. Track completion per environment in `docs/runbooks/observability-operations.md`.
7. **Grafana assets:** Import `docs/observability/dashboards/baseline.json` and `docs/observability/alerts/baseline.yaml` into Grafana Cloud, wiring them to PagerDuty (`clarivum-oncall`) and `#clarivum-alerts`, then link the panels in the runbook.
8. **Manual spans & synthetic follow-up:** Coordinate with feature owners to add manual spans for the critical flows noted in TSK-PLAT-003 notes and open/track the synthetic monitoring guardrail task once owners are assigned.
9. **Flagsmith automation secrets:** Add the `FLAGSMITH_PROJECT_ID`, `FLAGSMITH_API_TOKEN`, and optional `FLAGSMITH_PROJECT_DASHBOARD_URL` repo secrets/variables so `.github/workflows/flags-stale.yml` can run. Reuse/verify `SLACK_WEBHOOK_CI` for the workflow step. (Source: TSK-PLAT-002 rollout)
10. **Workflow smoke test:** After wiring the secrets, trigger the “Flagsmith Stale Flag Audit” workflow via the Actions tab (`workflow_dispatch`) to confirm Slack + GitHub issue outputs before relying on the Monday schedule. (Source: TSK-PLAT-002 DoD)
11. **Homepage rebuild brief:** Replace the placeholder in `src/app/page.tsx` with the next Clarivum homepage once product finalizes copy/structure. Capture the new scope in `docs/PRDs/requierments/ascii_designs/home.md` and list guardrails/tests to restore.
12. **CMS feed readiness check:** Prep Strapi collections for hero wizard, plan summaries, and learning slots before the rebuild so marketing can adjust copy without redeploying. Note the mapping in the same PRD when finalized.
13. **Learning slot instrumentation plan:** Decide how `/library` entries will surface on the rebuilt homepage and queue the analytics/guardrail updates alongside the implementation.
14. **Upstash cache & rate-limit secrets:** Verify `UPSTASH_CACHE_REST_URL`/`UPSTASH_CACHE_REST_TOKEN` and `UPSTASH_RATELIMIT_REST_URL`/`UPSTASH_RATELIMIT_REST_TOKEN` are populated for preview/staging/prod before rolling the shared cache. Record the check in `docs/runbooks/tools-platform-operations.md` under the UV widget section and file follow-ups if any environment needs new credentials. (Source: TSK-PLAT-048 next step)
15. **Terraform backend variables:** Populate `TF_BACKEND_BUCKET`, `TF_BACKEND_KEY`, `TF_BACKEND_REGION`, and `TF_BACKEND_DYNAMODB_TABLE` (plus optional `TERRAFORM_DEFAULT_WORKSPACE`) repository variables along with the `AWS_TERRAFORM_DEPLOYER_ROLE_ARN` secret so `.github/workflows/infra-ci.yml` can assume the TerraformDeployer role. (Source: TSK-PLAT-022 workflow hookup)
16. **Terraform static analysis follow-up:** Land shared `tflint` and `tfsec` configs for `infra/` and extend the new workflow to execute them, completing the remaining guardrails for TSK-PLAT-022.
17. **Terraform runbook reminder:** As part of a PR’s deployment checklist, explicitly include `-var-file=env/<env>.tfvars` when running `terraform plan` so the CLI doesn’t prompt for every required variable. Capture this in the release template/communication so the habit sticks. (Source: Strapi data foundation runbook update)
18. **Strapi GitHub variables/secrets:** Add `STRAPI_AWS_REGION`, `STRAPI_ECR_REGISTRY`, `STRAPI_ECR_REPOSITORY` repository variables plus the `strapi-dev`/`strapi-prod` environment variables (`STRAPI_CLUSTER_ARN`, `STRAPI_SERVICE_NAME`, `STRAPI_HEALTHCHECK_URL`, `STRAPI_REVALIDATE_URL`, optional `STRAPI_MIGRATION_COMMAND`, `STRAPI_DEPLOYMENT_WEBHOOK_URL`, `STRAPI_SMOKE_TEST_URLS`) and secrets (`AWS_STRAPI_DEPLOY_ROLE_ARN`, `STRAPI_REVALIDATE_SECRET`, `STRAPI_DEPLOYMENT_WEBHOOK_TOKEN`). If enabling blue/green, also set `STRAPI_CODEDEPLOY_APPLICATION`, `STRAPI_CODEDEPLOY_DEPLOYMENT_GROUP`, `STRAPI_TASK_DEFINITION_FAMILY`, and optional `STRAPI_CONTAINER_NAME`/`STRAPI_CONTAINER_PORT`. Track completion in the deployment runbook checklist so the attested deploy flow can run end-to-end. (Source: docs/runbooks/deployment.md §Strapi ECS deploys)
19. **Strapi workflow validation:** After wiring secrets/variables, trigger `.github/workflows/strapi-ci-cd.yml` via PR, push, and manual `workflow_dispatch` (for both `strapi-dev` and `strapi-prod`) to confirm the quality gate, CodeDeploy blue/green path (when configured), the new schema validator, and smoke checks all succeed. (Source: docs/runbooks/deployment.md checklist)
20. **Strapi guardrails:** Confirm the new ESLint/Vitest guardrails in `cms/` run end-to-end (`npm run strapi:ci`, `.github/workflows/strapi-ci-cd.yml`) and keep `cms/AGENTS.md` updated with owners as coverage grows. File follow-ups for any failing suites. (Source: cms/package.json scaffolding follow-up)
21. **Terraform tfvars real values:** Replace the example VPC/subnet/certificate/container/secret identifiers in `infra/strapi/env/dev.tfvars` and `infra/strapi/env/prod.tfvars` with the live ARNs or references stored in Secrets Manager, then run the documented `terraform -chdir=infra/strapi plan/apply -var-file=env/<env>.tfvars` path per `infra/strapi/AGENTS.md`. (Source: Strapi deployment readiness review)
22. **Strapi platform checklist:** Work through every unchecked item under §12 in `docs/PRDs/requierments/strapi/setup.md`—provision Postgres/S3, enable required plugins, wire SSO/RBAC, document environment variables, register webhooks, and record the backup drill + architecture updates—so the platform baseline is complete. (Source: docs/PRDs/requierments/strapi/setup.md)
23. **Strapi schema baseline:** Add the initial content-type and component schemas (homepage hero, plan summaries, learning slots, etc.) so the new schema validator exercises real definitions and the CMS can supply data for the upcoming rebuild. Reference the owning PRD/ADR when committing. (Source: Strapi CI/CD follow-up)
24. **Aurora tfvars real values:** Populate `aurora_cluster_identifier`, subnet group, parameter group, and Secrets Manager ARNs in `infra/app-data/env/dev.tfvars` and `infra/app-data/env/prod.tfvars` with the live values before the first apply. (Source: AWS migration follow-up)
25. **Aurora Terraform apply:** Review the generated plan artifact (`infra/app-data/dev.plan`), then run `terraform -chdir=infra/app-data apply dev.plan` (or re-run plan/apply with `-var-file=env/<env>.tfvars`) for each environment to materialise the Aurora cluster, parameter groups, and S3 buckets. Capture plan/apply artefacts in the PR comment. (Source: AWS migration follow-up)
26. **Aurora schema bootstrap:** Because the database is empty, execute `database/migrations/*.sql` against Aurora (via `psql` or the migration runner) to recreate the existing tables and confirm connections—no data migration required. (Source: AWS migration follow-up)
27. **Aurora secret sync:** After bootstrap, sync `/clarivum/aurora/<env>/*` Secrets Manager entries into ECS task definitions, Lambda environment variables, and local `.env` files per `docs/runbooks/secrets-management.md`, then validate Next.js + Strapi connectivity using the rotated credentials. (Source: AWS migration follow-up)
28. **UV widget env wiring:** Populate `STRAPI_API_URL` (or `STRAPI_BASE_URL`) and `STRAPI_TOOLS_UV_WIDGET_TOKEN` / `STRAPI_DELIVERY_API_TOKEN` for preview/staging/prod so the Strapi-backed copy loader stays online when the feature flag ships. Track completion per environment in `docs/runbooks/tools-platform-operations.md`. (Source: TSK-PLAT-047 follow-up)
29. **Document Strapi field mapping:** Once the `tools-uv-widget` collection is live, append the final field schema to the runbook changelog and notify the content team in `#clarivum-tools`. (Source: TSK-PLAT-047 follow-up)
30. **Strapi observability dashboards:** Stand up dashboards/alerts for Strapi health, latency, and error rates in Grafana (reuse OTLP streams from ADR-004) and document links in `docs/runbooks/deployment.md`. (Source: TSK-SHARED-003 follow-up)
31. **Strapi autoscaling policy:** Implement and document ECS Service autoscaling for Strapi (CPU/memory + request-based), updating Terraform and `docs/infra/cms-aws-overview.md` plus noting the guardrail in `docs/runbooks/deployment.md`. (Source: TSK-SHARED-003 follow-up)
32. **Plausible CI guardrail:** Once Plausible API credentials are stored in GitHub secrets, wire `npm run analytics:health` into a scheduled GitHub Action (daily) so Web Vitals ingestion failures page the team automatically. Document the workflow link in `docs/runbooks/seo-operations.md`. (Source: TSK-PLAT-051 follow-up)
33. **Web Vitals dashboard link:** Create the Plausible dashboard segmenting LCP/INP/CLS by route and attach the permanent link in `docs/runbooks/seo-operations.md` §Tooling once ready. (Source: TSK-PLAT-051 follow-up)
34. **Storybook hosting decision:** Choose and configure the hosted Storybook preview target (Chromatic vs AWS-hosted preview pipeline) with authentication before sharing the workbench broadly; document the access flow in `docs/PRDs/requierments/storybook/feature-requirements.md`. (Source: TSK-FE-004 follow-up)
35. **Composite story coverage:** Add stories for composite modules (hero wizard, CTA cards, testimonials) once those components land, keeping the story catalog aligned with current marketing flows. Track progress in `src/components/ui/__stories__/` and log updates in the component library ADR. (Source: TSK-FE-004 follow-up)
36. **Storybook test runner integration:** After the hosted preview decision, update `npm run storybook:test` to run the official Storybook test runner (including starting/stopping the server) and refresh documentation/CI wiring accordingly. (Source: TSK-FE-004 follow-up)
37. **Platform Terraform imports:** Use the `infra/aws/platform/dev.plan` diff to inventory existing AWS resources (VPC, subnets, API Gateway, Lambda, CloudFront, Route53, etc.) and run targeted `terraform -chdir=infra/aws/platform import` commands so the plan reflects the live estate before applying. Capture any irreconcilable drift in runbook notes. (Source: AWS platform migration follow-up)
38. **Platform Terraform apply:** Once imports reconcile, apply the platform stack from a trusted environment (`terraform -chdir=infra/aws/platform apply dev.plan` or re-run with `-var-file=env/<env>.tfvars`) and archive the plan/apply logs in the deployment record. Follow up with schema bootstrap and secret sync tasks. (Source: AWS platform migration follow-up)

## Nice-to-haves

- Add a short README section describing how `TODO.md` is maintained (e.g., link from `tasks/AGENTS.md`) if this list becomes long-term.
- Consider automating TODO capture via `npm run tasks:summary` so sprint health stays transparent.
